class Game {
    constructor(game, gameAttributes) {
        this.id = game.id
        this.title = gameAttributes.title
        this.cards = gameAttributes.cards
    }

    renderCards() {
        
    }
}