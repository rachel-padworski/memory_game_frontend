class Card {
    constructor(cards) {
        this.id = cards.id
        this.title = cards.title
        this.image_url = cards.image_url
        // Card.all.push(this)
        
    }


   
}
// Card.all = [];